# =============================================================================
#  custom/rules.conf — Window rules personalizadas
#  Adicione aqui as suas regras extras sem tocar em configs/windowrules.conf
# =============================================================================

# Exemplos:
# windowrule = float, class:^(mpv)$
# windowrule = opacity 0.9, class:^(code)$
